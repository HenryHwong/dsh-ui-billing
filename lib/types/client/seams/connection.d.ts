/**
 * Browser-side seam over the client Connection face: the only module naming
 * the transport module the balance channel is called through, so a transport
 * move stays a single-file edit.
 *
 * @module @huanghanheng/dsh-ui-billing/client/seams/connection
 */
export type { ClientConnectionRpc, ConnectionHandle, ConnectionRpcResult, } from '@deepseek-ai/dsh-client-connection/client';
//# sourceMappingURL=connection.d.ts.map